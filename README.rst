Pagerator — iterates over pages.
================================

This is a helper to build iterables when you need to fetch results already
divided into the pages.

For example, suppose, you want to fetch 100 latest commits from the Django's
repository:

.. include:: example.py
